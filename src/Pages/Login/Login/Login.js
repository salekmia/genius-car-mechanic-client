import React from 'react';

const Login = () => {
    return (
        <div>
            <h1>This is login</h1>
            <button className="btn btn-warning">Sign is with google</button>
        </div>
    );
};

export default Login;